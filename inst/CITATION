pkgVer <- function(pkg) {
    if (!exists("meta") || is.null(meta)) meta <- packageDescription(pkg)
    ver <- meta$Version
    paste0('https://github.com/edyp-lab/DaparToolshedData/', pkg, ' - R package version ', ver)
}

c(

    bibentry(bibtype="manual",
        title = "DaparToolshedData",
        author = personList(
            as.person("edyp-lab")
        ),
        year = format(Sys.Date(), "%Y"),
        url = "http://www.bioconductor.org/packages/DaparToolshedData",
        note = pkgVer('DaparToolshedData'),
        doi = '10.18129/B9.bioc.DaparToolshedData'
    ),
    bibentry(bibtype="article",
        title = "DaparToolshedData",
        author = personList(
            as.person("edyp-lab")
        ),
        year = format(Sys.Date(), "%Y"),
        journal = "bioRxiv",
        doi = "10.1101/TODO",
        url = "https://www.biorxiv.org/content/10.1101/TODO"
    )
)
